<Window x:Class="RaceTrackMooi.RaceStats"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RaceTrackMooi"
        mc:Ignorable="d"
        Title="RaceStats" Height="450" Width="800">
    <Window.DataContext>
        <local:DataContext/>
    </Window.DataContext>


    <Grid Background="DarkGray">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Label VerticalAlignment="Top" Content="{Binding TrackName}" Grid.Row= "0"></Label>
        <ListView Width="250" Height="180" ItemsSource="{Binding Drivers}" Grid.Row="1" VerticalAlignment="Top"></ListView>
    </Grid>
</Window>
